<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.5.0/font/bootstrap-icons.css">
  <link rel="stylesheet" href="/styles/bootstrap.min.journal.css">
  <link rel="stylesheet" href="/styles/style.css">
  <link rel="icon" type="image/svg+xml" href="/images/cup.svg">
  <title>Restaurant list (Express)</title>
</head>

<body>
  <nav class="navbar navbar-expand-md navbar-dark bg-primary">
    <div class="container-fluid">
      <a class="navbar-brand" {{#if isAuthenticated}} href="/" {{else}} href="/welcome" {{/if}} >
        <i class="bi bi-cup-straw"></i>
        Restaurant list
      </a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav"
        aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse justify-content-end" id="navbarNav">
        <ul class="navbar-nav align-items-end">
          {{#if isAuthenticated}}
          {{!-- md ≥768px --}}
          <div class="dropdown dropstart d-none d-md-block">
            <a class="text-decoration-none dropdown-toggle" id="dropdownMenu" data-bs-toggle="dropdown"
              aria-expanded="false">
              <div class="avatar-32 d-inline-block">
                <img class="rounded-circle w-100" src="{{navAvatar}}" alt="user avatar image">
              </div>
            </a>
            <ul class="dropdown-menu" aria-labelledby="dropdownMenu">
              <li>
                <span class="dropdown-item disabled d-inline-block" tabindex="-1" aria-disabled="true">
                {{navUsername}}<br>--
                </span>
              </li>
              <li><a class="dropdown-item" href="/user">帳戶管理</a></li>
              <li><a class="dropdown-item" href="/user/logout">登出</a></li>
            </ul>
          </div>
          {{!-- <768px --}}
          <li class="nav-item me-2 d-md-none">
            <a class="nav-link" href="/user">帳戶管理</a>
          </li>
          <li class="nav-item me-2 d-md-none">
            <a class="nav-link" href="/user/logout">登出</a>
          </li>
          {{else}}
          <li class="nav-item">
            <a class="nav-link" href="/user/register">註冊</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="/user/login">登入</a>
          </li>
          {{/if}}
        </ul>
      </div>
    </div>
  </nav>
  {{>formMessages}}
  {{{ body }}}
  <footer class="navbar-dark bg-primary text-light">
    <p>Copyright 2021 Charlie (Tzu Yin)</p>
  </footer>
  <script src="/scripts/bootstrap/bootstrap.bundle.min.js"></script>
  {{>scripts}}
</body>

</html>